{
  "id": "auto-memory",
  "name": "Auto Memory",
  "description": "Automatically extracts important facts and stores them in LanceDB with local embeddings for semantic search",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable or disable the auto-memory plugin",
        "default": true
      },
      "messageThreshold": {
        "type": "number",
        "description": "Number of messages after which to analyze and write to memory",
        "default": 5,
        "minimum": 1
      },
      "minImportance": {
        "type": "number",
        "description": "Minimum importance threshold (0-1) for facts to be saved",
        "default": 0.7,
        "minimum": 0,
        "maximum": 1
      },
      "notificationEnabled": {
        "type": "boolean",
        "description": "Whether to send a notification when memory is updated",
        "default": true
      },
      "notificationMessage": {
        "type": "string",
        "description": "Message to send as notification when memory is updated",
        "default": "💾 Memory updated"
      },
      "maxMessagesContext": {
        "type": "number",
        "description": "Maximum number of recent messages to include in the analysis context",
        "default": 30,
        "minimum": 1
      },
      "lancedb": {
        "type": "object",
        "description": "LanceDB configuration (required - plugin only works with embeddings)",
        "additionalProperties": false,
        "properties": {
          "dbPath": {
            "type": "string",
            "description": "Path to LanceDB database directory",
            "default": "~/.openclaw/memory/auto-memory-lancedb"
          }
        }
      },
      "embedding": {
        "type": "object",
        "description": "Local embedding configuration (required - plugin only works with embeddings)",
        "additionalProperties": false,
        "properties": {
          "modelPath": {
            "type": "string",
            "description": "Path to embedding model (GGUF format or HuggingFace URI)",
            "default": "hf:ggml-org/embeddinggemma-300M-GGUF/embeddinggemma-300M-Q8_0.gguf"
          },
          "modelCacheDir": {
            "type": "string",
            "description": "Directory to cache downloaded models",
            "default": "~/.cache/openclaw/models"
          }
        }
      }
    }
  }
}
